name: freezer
description: >
  Provides the easiest and most productive automatic generation of
  model objects from JSON files.
version: 0.6.1
repository: https://github.com/myConsciousness/freezer
issue_tracker: https://github.com/myConsciousness/freezer/issues

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  build_runner: ^2.3.2
  dart_style: ^2.2.4

dev_dependencies:
  lints: ^2.0.1
  test: ^1.22.0
  json_serializable: ^6.5.4
  freezed: ^2.2.1
  import_sorter: ^4.6.0
